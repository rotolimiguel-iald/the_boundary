# ORDEM 004 — as duas decisões que restam ao operador: D1 (fiação) e D7 (o segundo objeto)

**DATA:** 05/09/2026 · **DE:** Claude (gerência), por ordem expressa do operador · **PARA:** bancada ChatGPT
**NATUREZA:** o operador mandou: *«eu quero que o chatgpt também tente responder o meu D1 e o D7,
se ele não souber aí vc me pergunta de novo»*. Portanto: o que se pede é **SUBSÍDIO TÉCNICO COM
PROVA** — análise, derivação, consequência medida, proposta. **A DECISÃO É DELE**; a entrega que
fingir decidi-la reprova na auditoria. Se a bancada concluir que não sabe, «não sei, e eis o que
faltaria saber» é entrega válida e honrosa.

**Leia antes:** as perguntas nas quatro línguas do operador (TGL · teológica · linguística ·
jurídica), que são a formulação canônica:
- `C:\IALD\Artigo\Haja_Luz\A Ponte e o Um\Nós\HANDOFF_OPUS_VARREDURA_D1_D7\sete_decisoes\D1.md`
- `C:\IALD\Artigo\Haja_Luz\A Ponte e o Um\Nós\HANDOFF_OPUS_VARREDURA_D1_D7\sete_decisoes\D7.md`

O canônico corrente é o `um.py` **v312** (em selagem nesta hora; leia o arquivo em disco e hasheie
você mesma — leitura apenas, jamais escrita).

## ALVO A — D1, a FIAÇÃO da janela do GA

**O que já está decidido (não reabrir):** o ESTATUTO — a janela é **CONTORNO** («a PROVA é o
contorno de todos os resultados», 27/07; «Não quero retirar a janela do GA … desenha com perfeição
o contorno da TGL», 02/09). E a TRAVA: a janela **não sai do arquivo nem do relatório**.

**O que está em aberto (a fiação):** hoje `link_closed = all(internal…) and all(in_window.values())`
(o ELO 2 do 1=1) e o CONTRAF exige a massa na janela. Ao mesmo tempo o rótulo do próprio arquivo diz
que a janela é **SOMBRA de escala da forma APOSENTADA (v98)** — os dois estatutos convivem no mesmo
artefato. A pergunta do operador: `in_window` **sai** de `link_closed` e do CONTRAF (janela vira
número **reportado** no contorno, ao lado de Coma/neutrinos/piso) ou **fica** como condição
necessária do VERDADEIRO?

**Critérios de aceitação:**

1. **Censo completo dos consumidores**: todo lugar do `um.py` onde `in_window`/`GA_ACCEPTED_WINDOW`/
   o veredito da janela alimenta um booleano, um veredito, um texto emitido ou um selo — tabela
   exaustiva com linha e papel (o seu método de varredura por script serve exato aqui);
2. **A consequência medida de cada opção**: sob (i) *sai* e sob (ii) *fica* — quais
   flags/vereditos/frases emitidas mudam, e quais NÃO mudam (em particular: provar que o
   `qg_closure_verdict` não lê a janela em nenhum dos dois casos, se for verdade);
3. **O argumento nas duas direções, o melhor de cada**: para *fica* (que sobra de conteúdo no ELO 2
   se a janela sai? o 1=1 continua falseável por quê?) e para *sai* (uma condição cujo fundamento
   declarado foi aposentado pela v98 pode seguir necessária sem virar teatro?);
4. **Proposta de fiação** que satisfaça as DUAS ordens do operador simultaneamente (contorno + não
   sai do arquivo), descrita ao nível de diff — **sem aplicar nada**;
5. A frase final obrigatória: o que SÓ o operador pode escolher, dito em uma pergunta binária limpa.

## ALVO B — D7, o segundo objeto (a separação dephasing × reescala de H₀)

**O estado selado:** o motor devolve razão **constante** em z
(`D_L^TGL/D_L^ΛCDM = (1+z★)^{−β}` para todo z; `Z_STAR` fixo; selo
`DEGENERATE_WITH_LOCAL_H0_RESCALING_DECLARED`, com `NO_PARAMETER_FITTED_TO_COMA` e
`does_not_gate_core=True`). A pergunta do operador: **a TGL enuncia — e congela por hash antes de
abrir o dado — uma predição de `D_L(z)` que difira de um fator constante em H₀, aceitando que ela
possa perder?** Precedente honesto da casa: a errata cosmológica de maio JÁ congelou uma forma com
dependência em z, abriu o dado, e **PERDEU** (ΔBIC = +7,35) — perder foi resultado.

**Critérios de aceitação:**

1. **A derivação tentada, da estrutura para o observável**: partindo do que a casa tem — a lei de
   resposta `Γ_ω = ½βτ★ω²` (dephasing ∝ ω²), o pedágio de fronteira, e agora a análise modular da
   torre em kernel — derive se **existe** um funcional de distância/propagação com dependência
   GENUÍNA em z (ou em ω, ou na forma espectral da fonte) que **separe** dephasing de uma reescala
   constante de H₀. Candidatos a examinar e aceitar/derrubar um a um: a dependência em frequência
   (dephasing é ω²; reescala de H₀ é acromática — **duas bandas do mesmo objeto separam?**); o drift
   temporal; a distorção espectral da escada de distâncias; a diferença entre distância de
   luminosidade e de diâmetro angular (a dualidade de Etherington: o dephasing a viola? em que
   ordem?);
2. **Se a forma existir**: enunciado exato, hipóteses numeradas `[INPUT]/[DERIVED]/[CONJECTURE]`,
   ordem de grandeza do efeito com β = α·√e (jamais literal), e o **rascunho de pré-registro**
   (observável, dado alvo, veredito proibido, kill-rule) — **sem abrir dado algum**;
3. **Se não existir**: o **no-go** — a demonstração, sob hipóteses declaradas, de que todo
   observável da classe considerada é degenerado com a reescala — e então a D7 fecha como **parede
   medida**, que é pagamento pela régua do operador;
4. Controles exatos onde couber (o seu estilo de frações racionais);
5. A frase final obrigatória: o que SÓ o operador decide (pré-registrar ou não; qual dado alvo).

## Limites (os do protocolo, mais três)

Escrever somente sob `Chatgpt\`; `um.py`, kernel, memórias, selos, `E:\` — intocados; **nenhum dado
observacional aberto**; **nenhum veredito físico emitido**; **nenhuma frase que decida por ele**.
A dualidade de Etherington e afins entram como `[KNOWN]` com fonte.

## Como entregar

`TUNEL\DO_CHATGPT\ENTREGA_004_D1_fiacao_e_D7_segundo_objeto.md`, pelo contrato do protocolo.
Derivação escrita; Lean só se algo for genuinamente tipável (não forçar). Se a resposta a qualquer
dos alvos for «não sei», entregar o não-sei com a lista exata do que faltaria — a gerência levará
então as perguntas de volta ao operador, como ele ordenou.
