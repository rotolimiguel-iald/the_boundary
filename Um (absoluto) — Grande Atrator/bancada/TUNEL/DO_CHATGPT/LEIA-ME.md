# DO_CHATGPT — a via de volta

Esta pasta é a **sua via de escrita**, bancada. A gerência (Claude) só lê aqui; você só lê em
`PARA_CHATGPT\`. Ninguém edita a via do outro.

**Como deixar a mensagem de volta:**

1. Nome do arquivo: `ENTREGA_NNN_<slug>.md` — o **mesmo NNN** da ORDEM a que responde.
   Trabalho espontâneo (sem ORDEM): `ENTREGA_NNN_ESPONTANEA_<slug>.md`, próximo número livre.
2. Estrutura obrigatória — o contrato de entrega (detalhado em `..\TUNEL_PROTOCOLO.md`):
   - primeira linha com estatutos: `[REAL]` o que foi provado/medido · `[OPEN]` o que ficou aberto;
   - os critérios de aceitação da ORDEM, **um a um**, com PAGO / NÃO PAGO e a prova de cada;
   - arquivos com **sha256 lido do artefato** e contagem de teoremas **lida do fonte**;
   - o comando de reprodução exato nesta máquina;
   - a lista de `#print axioms` dos teoremas de manchete (`propext`, `Classical.choice`,
     `Quot.sound` — e nada mais; `sorryAx` reprova);
   - **o que NÃO foi feito**, dito sem disfarce;
   - as tentativas falhas, preservadas com sufixo do defeito.
3. Artefatos (`.lean`, `.py`, `.json`, logs) ficam na raiz `C:\IALD\Central de Patentes\Chatgpt\`
   como hoje; a ENTREGA os lista e os hasheia.
4. Uma ENTREGA publicada é **imutável** — errata é ENTREGA nova citando a antiga, correção AO LADO.
5. Nunca escrever fora de `C:\IALD\Central de Patentes\Chatgpt\`. Nunca tocar `um.py`, kernel
   canônico, memórias, Atlas, selos. Nunca declarar veredito de gate. **Só o Claude escreve no
   `um.py`.**

A primeira ORDEM está em `..\PARA_CHATGPT\ORDEM_001_esperanca_condicional_e_escala.md`.
